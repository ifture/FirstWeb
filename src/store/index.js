import { createStore } from 'vuex'
import home from './home'
import foot from './foot'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    home: home,
    foot: foot,
  }
})
